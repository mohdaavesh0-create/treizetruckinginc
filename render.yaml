services:
  - type: static_site
    name: treize-trucking-inc
    runtime: static
    buildCommand: echo "Static site - no build needed"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
